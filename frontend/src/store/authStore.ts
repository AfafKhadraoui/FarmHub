// Auth store using Zustand
// TODO: Install zustand first: npm install zustand

export const useAuthStore = () => {
  // Placeholder until zustand is installed
  return {
    user: null,
    setUser: () => {},
    clearUser: () => {},
  };
};
